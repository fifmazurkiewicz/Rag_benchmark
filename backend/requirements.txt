fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# LLM + embeddings
anthropic>=0.28.0
openai>=1.23.0

# Vector stores
qdrant-client>=1.9.0
chromadb>=0.5.0
neo4j>=5.20.0

# Graph RAG
graphrag-sdk[litellm]>=1.0.0

# Chunking / NLP
sentence-transformers>=2.7.0
transformers>=4.40.0
torch>=2.2.0

# PDF parsing (TOiP bank documents) — Docling primary, pdfplumber fallback
docling>=2.0.0
pdfplumber>=0.11.0

# Excel export
openpyxl>=3.1.0

# Reranking (cohere kept for direct API option; openrouter reranker uses openai package)
cohere>=5.0.0

# Obsidian vault parsing
pyyaml>=6.0.1

# Dataset sources (HuggingFace loaders)
datasets>=2.19.0

# Evaluation
ragas>=0.1.14
deepeval>=0.21.0
